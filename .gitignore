# Tooling leftovers — not part of the site
.agents/
agent/
.claude/
skills-lock.json

# Editor / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
*.swp
*~

# Backups and scratch
*.bak
*.orig
*.tmp
scratch/

# The untouched camera originals: 43 MB of HEIC/PNG kept locally as the
# master copies. The web-sized JPEGs beside them are what ships.
photos/_originals/
photos/_manifest.json
